1. 先写 CMakeLists.txt：先把现有的 4 个模块跑通，生成一个 lib 库。
2. 写 MprpcApplication：把 RpcConfig 集成进去，作为初始化的入口。
3. 写 ZookeeperUtil：这是最核心的增量功能。
4. 修改 RpcProvider 和 MprpcChannel：
    引入 MprpcApplication 获取配置。
    引入 ZookeeperUtil 进行注册和发现。
5. 最后写 Logger：等框架跑通了，再把 std::cout 替换成异步日志。